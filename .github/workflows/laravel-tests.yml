name: Run tests

on:
  push:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  tests:
    uses: Kehet/shared-github-workflows/.github/workflows/laravel-tests.yml@master
    with:
      php-version: '8.3'

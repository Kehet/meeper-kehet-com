name: Laravel tests

on:
  push:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

jobs:
  laravel-tests:
    uses: Kehet/shared-github-workflows/.github/workflows/laravel-tests.yml@master
    with:
      php-version: '8.3'

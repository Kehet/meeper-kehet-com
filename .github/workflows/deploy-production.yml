name: Production Deployment

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write

concurrency: production_environment

jobs:
  deploy:
    if: github.ref == 'refs/heads/master'
    uses: Kehet/shared-github-workflows/.github/workflows/dependabot-auto-merge.yml@master
    with:
      php-version: '8.3'
